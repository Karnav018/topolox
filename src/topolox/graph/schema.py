"""Kùzu graph schema (Cypher DDL). Phase 1."""

from __future__ import annotations

# Node and relationship table definitions are added here in Phase 1, e.g.
#   CREATE NODE TABLE Symbol(id STRING, kind STRING, ..., PRIMARY KEY(id))
#   CREATE REL TABLE CALLS(FROM Symbol TO Symbol)
SCHEMA_STATEMENTS: tuple[str, ...] = ()
