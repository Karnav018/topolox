"""LanceDB table schema for symbol rows. Phase 1."""

from __future__ import annotations

# The symbol table holds: id, path, text, and the embedding vector column.
# Defined here in Phase 1 once the embedding dimension is known.
